{
  /* <View style={{ width: "40%" }}>
{cryptos.map((currency) => (
  <Card
    title={`${currency.rank}. ${currency.name}`}
    key={currency.id}
    extra={<img style={{ width: "100%" }} src={currency.iconUrl} />}
  >
    <Text>yes</Text>
  </Card>
))}
</View> */
}
